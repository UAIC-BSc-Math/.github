# Maintenance

Repository maintenance rules:

- never commit from the archive root
- each course folder is its own git repository after publishing
- do not run root-level `git add .`
- do not force-push course repositories
- do not make repositories public without a rights review
- use the export plan before further publishing
- use the issue templates for metadata and material fixes
- review LFS and large-file warnings before publishing large repositories
