name: Markdown Link Check

on:
  push:
  pull_request:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder markdown link check
        run: |
          echo "Template placeholder. Replace with a dedicated link-check step when ready."
